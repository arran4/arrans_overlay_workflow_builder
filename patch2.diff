<<<<<<< SEARCH
[[ define "ebuild_revision_and_manifest" ]]
              clean_version="${version#${{ env.epn }}-}"
              clean_version="${clean_version%.ebuild}"
              clean_version="${clean_version%.tmp}"
              clean_version="${clean_version#v}"
              if next_version=$(g2 ebuild next-revision --inspect "$tmp_ebuild_file" "${ebuild_dir}" "${clean_version}"); then
                  next_ebuild_file="${ebuild_dir}/${{ env.epn }}-${next_version}.ebuild"
=======
[[ define "ebuild_revision_and_manifest" ]]
              if next_version=$(g2 ebuild next-revision --inspect "$tmp_ebuild_file" "${ebuild_dir}" "${version}"); then
                  next_ebuild_file="${ebuild_dir}/${{ env.epn }}-${next_version}.ebuild"
>>>>>>> REPLACE
