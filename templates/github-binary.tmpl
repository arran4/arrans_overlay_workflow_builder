# Generated by arrans_binary_overlay_builder
EAPI=8
DESCRIPTION="[[ .Description ]]"
HOMEPAGE="[[ .Homepage ]]"
LICENSE="[[ .License ]]"
SLOT="0"
KEYWORDS="[[ .MaskedKeywords ]]"
IUSE="[[ range $use, $archs := $.ReverseProgramsAsAlternatives]] [[ $use | UseFlagSafe ]][[ end ]][[ if $.HasManualPages ]] man[[ end ]][[ if $.HasDocuments ]] doc[[ end ]][[ range $shell := $.ShellCompletionShells ]] [[ $shell | UseFlagSafe ]][[ end ]]"
REQUIRED_USE="[[ range $use, $archs := $.ReverseProgramsAsAlternatives]][[ $use | UseFlagSafe ]]? ( || ( [[ range $arch := $archs]][[ $arch ]] [[ end ]] ) ) [[ end ]]"
DEPEND=""
RDEPEND="[[ range $i, $dep := .MainDependencies]][[ $dep ]] [[ end ]][[ range $prog, $deps := .AlternativeDependencies]][[ if gt (len $deps) 0 ]] [[ $prog ]]? ( [[ range $dep := $deps]][[ $dep ]] [[ end ]] ) [[ end ]][[ end ]]"
S="${WORKDIR}"

SRC_URI="
[[ range $er := .ExternalResources ]]
  [[ range $i, $uf := $er.MustHaveUseFlags ]][[ $uf | UseFlagSafe ]]? ( [[ end ]][[ range $i, $uf := $er.MustntHaveUseFlags ]]![[ $uf | UseFlagSafe ]]? ( [[ end ]]https://github.com/[[ $.GithubOwner ]]/[[ $.GithubRepo ]]/releases/download/[[ $.Tag ]]/[[ $er.ReleaseFilename | ebuildvardoublequoted ]] [[ range $i, $uf := $er.MustHaveUseFlags ]] ) [[ end ]][[ range $i, $uf := $er.MustntHaveUseFlags ]] ) [[ end ]]
[[ end ]]
"

src_unpack() {
[[ range $er := .ExternalResources ]]
  [[- if $er.Archived ]]
    [[ $count := 0 ]]
    if [[range $i, $uf := $er.MustHaveUseFlags]][[ if gt $count 0]] && [[ end ]][[ $count = 1 ]]use [[ $uf | UseFlagSafe ]][[end]][[range $i, $uf := $er.MustntHaveUseFlags]][[ if gt $count 0]] && [[ end ]][[ $count = 1 ]]! use [[ $uf | UseFlagSafe ]] [[end]]; then
        unpack "${DISTDIR}/[[ $er.ReleaseFilename | ebuildvardoublequoted ]]"
    fi
  [[- end ]]
[[ end ]]
[[ if $.HasCompressedManualPages ]]
  if use man; then
    [[- range $mans := $.CompressedManualPages ]]
      [[- if ne $mans.Keyword "" ]]
    if use [[ $mans.Keyword ]]; then
      [[- range $man := $mans.Grouped ]]
        [[ $man.Decompressor ]] "[[ $man.SourceFilepath ]]" || die "Failed to decompress manual page [[ $man.DestinationFilename ]]"
      [[- end ]]
    fi
      [[- else ]]
      [[- range $man := $mans.Grouped ]]
        [[ $man.Decompressor ]] "[[ $man.SourceFilepath ]]" || die "Failed to decompress manual page [[ $man.DestinationFilename ]]"
      [[- end ]]
      [[- end ]]
    [[- end ]]
  fi
[[ end ]]
}

src_install() {
    exeinto /opt/bin
[[ range $pname, $prog := .Programs ]]
[[ range $keyword, $binary := $prog.Binary ]]
[[ if gt (len $binary) 2 ]]
    [[ $count := 0 ]]
    if [[range $i, $uf := $.GetMustHaveUseFlags $pname $keyword]][[ if gt $count 0]] && [[ end ]][[ $count = 1 ]]use [[ $uf | UseFlagSafe ]][[end]][[range $i, $uf := $.GetMustntHaveUseFlags $pname $keyword]][[ if gt $count 0]] && [[ end ]][[ $count = 1 ]]! use [[ $uf | UseFlagSafe ]] [[end]]; then
        newexe "[[ index $binary 1 ]]" "[[ $prog.InstalledFilename ]]" || die "Failed to install binary"
    fi
[[ else ]]
    [[ $count := 0 ]]
    if [[range $i, $uf := $.GetMustHaveUseFlags $pname $keyword]][[ if gt $count 0]] && [[ end ]][[ $count = 1 ]]use [[ $uf | UseFlagSafe ]][[end]][[range $i, $uf := $.GetMustntHaveUseFlags $pname $keyword]][[ if gt $count 0]] && [[ end ]][[ $count = 1 ]]! use [[ $uf | UseFlagSafe ]] [[end]]; then
        newexe "${DISTDIR}/[[ index $binary 0 | ebuildvardoublequoted ]]" "[[ $prog.InstalledFilename ]]" || die "Failed to install binary"
    fi
[[ end ]]
[[ end ]]
[[ end ]]

[[ range $shell := $.ShellCompletionShells ]]
  if use [[ $shell ]]; then
    insinto "[[ $.ShellCompletionInstallPath $shell ]]"
    [[- range $files := $.ShellCompletion $shell ]]
      [[- if ne $files.Keyword "" ]]
    if use [[ $files.Keyword ]]; then
      [[- range $file := $files.Grouped ]]
        newins "[[ $file.SourceFilepath ]]" "[[ $file.DestinationFilename ]]" || die "Failed to shell completion file"
      [[- end ]]
    fi
      [[- else ]]
      [[- range $file := $files.Grouped ]]
        newins "[[ $file.SourceFilepath ]]" "[[ $file.DestinationFilename ]]" || die "Failed to shell completion file"
      [[- end ]]
      [[- end ]]
    [[- end ]]
  fi
[[ end ]]

[[ if $.HasManualPages ]]
  if use man; then
    [[- range $mans := $.ManualPages ]]
      [[- if ne $mans.Keyword "" ]]
    if use [[ $mans.Keyword ]]; then
      [[- range $man := $mans.Grouped ]]
        newman "[[ $man.UncompressedSourceFilepath ]]" "[[ $man.DestinationFilename ]]" || die "Failed to install manual page [[ $man.DestinationFilename ]]"
      [[- end ]]
    fi
      [[- else ]]
      [[- range $man := $mans.Grouped ]]
        newman "[[ $man.UncompressedSourceFilepath ]]" "[[ $man.DestinationFilename ]]" || die "Failed to install manual page [[ $man.DestinationFilename ]]"
      [[- end ]]
      [[- end ]]
    [[- end ]]
  fi
[[ end ]]
[[ if $.HasDocuments ]]
  if use doc; then
    [[- range $docs := $.Documents ]]
      [[- if ne $docs.Keyword "" ]]
    if use [[ $docs.Keyword ]]; then
      [[- range $doc := $docs.Grouped ]]
        newdoc "[[ $doc.SourceFilepath ]]" "[[ $doc.DestinationFilename ]]" || die "Failed to install document [[ $doc.DestinationFilename ]]"
      [[- end ]]
    fi
      [[- else ]]
      [[- range $doc := $docs.Grouped ]]
        newdoc "[[ $doc.SourceFilepath ]]" "[[ $doc.DestinationFilename ]]" || die "Failed to install document [[ $doc.DestinationFilename ]]"
      [[- end ]]
      [[- end ]]
    [[- end ]]
  fi
[[ end ]]
}
